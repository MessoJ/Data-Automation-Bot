@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    scroll-behavior: smooth;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }

  body {
    @apply bg-gradient-to-br from-primary-50 to-info-50 min-h-screen;
  }
}

@layer components {
  /* Glassmorphism Effect */
  .glass {
    @apply bg-white/25 backdrop-blur-lg border border-white/20;
  }

  .glass-card {
    @apply glass rounded-3xl shadow-2xl;
  }

  /* Premium Button Styles */
  .btn-primary {
    @apply inline-flex items-center gap-2 px-6 py-3 rounded-xl font-semibold text-white 
           bg-gradient-to-r from-primary-500 to-primary-600 
           hover:from-primary-600 hover:to-primary-700
           transform hover:-translate-y-0.5 hover:shadow-lg hover:shadow-primary-500/40
           transition-all duration-250 cursor-pointer relative overflow-hidden;
  }

  .btn-secondary {
    @apply inline-flex items-center gap-2 px-6 py-3 rounded-xl font-semibold 
           bg-white/80 text-neutral-700 border border-white/30 backdrop-blur-sm
           hover:bg-white/95 hover:-translate-y-0.5 hover:shadow-lg
           transition-all duration-250 cursor-pointer;
  }

  .btn-glow::before {
    content: '';
    @apply absolute top-0 left-[-100%] w-full h-full 
           bg-gradient-to-r from-transparent via-white/40 to-transparent
           transition-left duration-500;
  }

  .btn-glow:hover::before {
    @apply left-full;
  }

  /* Card Styles */
  .card {
    @apply bg-white rounded-2xl p-6 shadow-lg border border-neutral-200 
           hover:shadow-xl hover:-translate-y-1 transition-all duration-300;
  }

  .kpi-card {
    @apply card relative overflow-hidden min-h-[200px];
  }

  .kpi-card::before {
    content: '';
    @apply absolute -top-1/2 -right-1/2 w-full h-full rounded-full opacity-5;
  }

  .kpi-card.revenue::before {
    @apply bg-success-500;
  }

  .kpi-card.orders::before {
    @apply bg-info-500;
  }

  .kpi-card.inventory::before {
    @apply bg-warning-500;
  }

  .kpi-card.platforms::before {
    @apply bg-primary-500;
  }

  /* Loading Animation */
  .loading-ring {
    @apply border-4 border-transparent border-t-primary-500 rounded-full animate-spin;
  }

  .loading-ring:nth-child(2) {
    @apply border-t-success-500 animate-spin;
    animation-delay: -0.5s;
  }

  .loading-ring:nth-child(3) {
    @apply border-t-warning-500 animate-spin;
    animation-delay: -1s;
  }

  /* Activity Items */
  .activity-item {
    @apply flex items-center gap-4 p-4 rounded-xl border-l-4 
           hover:bg-neutral-50 transition-all duration-200;
  }

  .activity-item.success {
    @apply border-l-success-500;
  }

  .activity-item.warning {
    @apply border-l-warning-500;
  }

  .activity-item.error {
    @apply border-l-error-500;
  }

  .activity-item.info {
    @apply border-l-info-500;
  }
}

@layer utilities {
  .text-gradient {
    @apply bg-gradient-to-r from-primary-500 to-success-500 bg-clip-text text-transparent;
  }

  .animate-entrance {
    @apply animate-slide-up;
  }

  .floating-shape {
    @apply absolute rounded-full opacity-10 animate-float;
  }

  .floating-shape-1 {
    @apply w-15 h-15 bg-primary-500 top-1/4 left-1/12;
  }

  .floating-shape-2 {
    @apply w-20 h-20 bg-success-500 top-1/2 right-1/6;
    animation-delay: -5s;
  }

  .floating-shape-3 {
    @apply w-10 h-10 bg-info-500 bottom-1/3 left-3/5;
    animation-delay: -10s;
  }

  .floating-shape-4 {
    @apply w-6 h-6 bg-warning-500 top-1/6 right-1/2;
    animation-delay: -15s;
  }
}