{% extends "base.html" %}

{% block title %}Error {{ error_code }} - E-commerce Data Automation{% endblock %}

{% block content %}
<div class="error-page">
    <div class="error-container">
        <div class="error-animation">
            <div class="error-icon">
                {% if error_code == 404 %}
                    🔍
                {% elif error_code == 500 %}
                    ⚠️
                {% else %}
                    ❌
                {% endif %}
            </div>
            <div class="floating-elements">
                <div class="element element-1"></div>
                <div class="element element-2"></div>
                <div class="element element-3"></div>
            </div>
        </div>
        
        <div class="error-content">
            <h1 class="error-title">{{ error_code }}</h1>
            <h2 class="error-subtitle">
                {% if error_code == 404 %}
                    Page Not Found
                {% elif error_code == 500 %}
                    Internal Server Error
                {% else %}
                    Something Went Wrong
                {% endif %}
            </h2>
            <p class="error-message">
                {% if error_message %}
                    {{ error_message }}
                {% elif error_code == 404 %}
                    The page you're looking for doesn't exist or has been moved.
                {% elif error_code == 500 %}
                    We're experiencing some technical difficulties. Please try again later.
                {% else %}
                    An unexpected error occurred. Our team has been notified.
                {% endif %}
            </p>
            
            <div class="error-suggestions">
                {% if error_code == 404 %}
                <div class="suggestion">
                    <div class="suggestion-icon">💡</div>
                    <div class="suggestion-text">
                        <strong>Suggestions:</strong>
                        <ul>
                            <li>Check the URL for typos</li>
                            <li>Go back to the previous page</li>
                            <li>Visit our dashboard homepage</li>
                        </ul>
                    </div>
                </div>
                {% elif error_code == 500 %}
                <div class="suggestion">
                    <div class="suggestion-icon">🔧</div>
                    <div class="suggestion-text">
                        <strong>What you can do:</strong>
                        <ul>
                            <li>Refresh the page in a few minutes</li>
                            <li>Contact support if the issue persists</li>
                            <li>Check our status page</li>
                        </ul>
                    </div>
                </div>
                {% endif %}
            </div>
            
            <div class="error-actions">
                <button class="premium-btn primary" onclick="goHome()">
                    <span class="btn-icon">🏠</span>
                    <span class="btn-text">Go to Dashboard</span>
                </button>
                <button class="premium-btn secondary" onclick="goBack()">
                    <span class="btn-icon">←</span>
                    <span class="btn-text">Go Back</span>
                </button>
                <button class="premium-btn secondary" onclick="refreshPage()">
                    <span class="btn-icon">🔄</span>
                    <span class="btn-text">Refresh</span>
                </button>
            </div>
            
            <div class="help-section">
                <p class="help-text">Need additional help?</p>
                <div class="help-links">
                    <a href="/reports" class="help-link">📊 Reports</a>
                    <a href="/jobs" class="help-link">⚙️ Jobs</a>
                    <a href="/config" class="help-link">🔧 Settings</a>
                    <a href="mailto:support@data-automation-bot.com" class="help-link">📧 Support</a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.error-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-6);
    background: linear-gradient(135deg, var(--neutral-50), var(--primary-50));
}

.error-container {
    max-width: 600px;
    width: 100%;
    text-align: center;
    background: var(--bg-primary);
    border-radius: var(--radius-3xl);
    padding: var(--space-12);
    box-shadow: var(--shadow-2xl);
    border: 1px solid var(--neutral-200);
    position: relative;
    overflow: hidden;
}

.error-animation {
    position: relative;
    margin-bottom: var(--space-8);
}

.error-icon {
    font-size: 6rem;
    margin-bottom: var(--space-4);
    animation: bounce 2s ease-in-out infinite;
}

@keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
        transform: translateY(0);
    }
    40% {
        transform: translateY(-20px);
    }
    60% {
        transform: translateY(-10px);
    }
}

.floating-elements {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
}

.element {
    position: absolute;
    width: 20px;
    height: 20px;
    background: var(--primary-color);
    border-radius: var(--radius-full);
    opacity: 0.1;
    animation: float 6s ease-in-out infinite;
}

.element-1 {
    top: 20%;
    left: 20%;
    animation-delay: 0s;
}

.element-2 {
    top: 60%;
    right: 20%;
    animation-delay: 2s;
}

.element-3 {
    bottom: 20%;
    left: 50%;
    animation-delay: 4s;
}

.error-content {
    position: relative;
    z-index: 2;
}

.error-title {
    font-size: 4rem;
    font-weight: var(--font-extrabold);
    color: var(--primary-color);
    margin-bottom: var(--space-2);
    line-height: 1;
}

.error-subtitle {
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
    color: var(--neutral-800);
    margin-bottom: var(--space-4);
}

.error-message {
    font-size: var(--text-lg);
    color: var(--neutral-600);
    margin-bottom: var(--space-6);
    line-height: var(--leading-relaxed);
}

.error-suggestions {
    background: var(--neutral-50);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
    margin-bottom: var(--space-8);
    border: 1px solid var(--neutral-200);
}

.suggestion {
    display: flex;
    align-items: flex-start;
    gap: var(--space-4);
    text-align: left;
}

.suggestion-icon {
    font-size: var(--text-2xl);
    flex-shrink: 0;
    margin-top: var(--space-1);
}

.suggestion-text {
    flex: 1;
}

.suggestion-text strong {
    font-size: var(--text-base);
    color: var(--neutral-800);
    display: block;
    margin-bottom: var(--space-2);
}

.suggestion-text ul {
    list-style: none;
    padding: 0;
}

.suggestion-text li {
    font-size: var(--text-sm);
    color: var(--neutral-600);
    margin-bottom: var(--space-1);
    position: relative;
    padding-left: var(--space-4);
}

.suggestion-text li:before {
    content: '•';
    color: var(--primary-color);
    position: absolute;
    left: 0;
    font-weight: var(--font-bold);
}

.error-actions {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    margin-bottom: var(--space-8);
    flex-wrap: wrap;
}

.help-section {
    border-top: 1px solid var(--neutral-200);
    padding-top: var(--space-6);
}

.help-text {
    font-size: var(--text-sm);
    color: var(--neutral-600);
    margin-bottom: var(--space-4);
}

.help-links {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    flex-wrap: wrap;
}

.help-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1);
    padding: var(--space-2) var(--space-3);
    background: var(--neutral-100);
    color: var(--neutral-700);
    text-decoration: none;
    border-radius: var(--radius-lg);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    transition: all var(--transition-fast);
}

.help-link:hover {
    background: var(--primary-color);
    color: var(--bg-primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
}

@media (max-width: 768px) {
    .error-container {
        padding: var(--space-8);
        margin: var(--space-4);
    }
    
    .error-title {
        font-size: 3rem;
    }
    
    .error-subtitle {
        font-size: var(--text-xl);
    }
    
    .error-message {
        font-size: var(--text-base);
    }
    
    .error-actions {
        flex-direction: column;
        align-items: center;
    }
    
    .premium-btn {
        width: 100%;
        max-width: 200px;
    }
    
    .help-links {
        flex-direction: column;
        align-items: center;
    }
    
    .help-link {
        width: 100%;
        max-width: 200px;
        justify-content: center;
    }
}

@media (max-width: 480px) {
    .error-icon {
        font-size: 4rem;
    }
    
    .error-title {
        font-size: 2.5rem;
    }
    
    .suggestion {
        flex-direction: column;
        text-align: center;
        gap: var(--space-2);
    }
}

/* Dark mode support */
@media (prefers-color-scheme: dark) {
    .error-page {
        background: linear-gradient(135deg, var(--neutral-900), var(--neutral-800));
    }
    
    .error-container {
        background: var(--neutral-800);
        border-color: var(--neutral-700);
        color: var(--neutral-100);
    }
    
    .error-subtitle {
        color: var(--neutral-100);
    }
    
    .error-message {
        color: var(--neutral-300);
    }
    
    .error-suggestions {
        background: var(--neutral-700);
        border-color: var(--neutral-600);
    }
    
    .suggestion-text strong {
        color: var(--neutral-100);
    }
    
    .suggestion-text li {
        color: var(--neutral-300);
    }
    
    .help-section {
        border-color: var(--neutral-600);
    }
    
    .help-text {
        color: var(--neutral-400);
    }
    
    .help-link {
        background: var(--neutral-700);
        color: var(--neutral-200);
    }
    
    .help-link:hover {
        background: var(--primary-color);
        color: var(--neutral-900);
    }
}

/* Accessibility improvements */
@media (prefers-reduced-motion: reduce) {
    .error-icon,
    .element {
        animation: none;
    }
}
</style>

<script>
function goHome() {
    window.location.href = '/web/';
}

function goBack() {
    if (window.history.length > 1) {
        window.history.back();
    } else {
        goHome();
    }
}

function refreshPage() {
    window.location.reload();
}

// Add some sparkle to the error page
document.addEventListener('DOMContentLoaded', function() {
    // Create sparkle effect on mouse move
    document.addEventListener('mousemove', function(e) {
        if (Math.random() > 0.95) {
            createSparkle(e.pageX, e.pageY);
        }
    });
    
    function createSparkle(x, y) {
        const sparkle = document.createElement('div');
        sparkle.style.cssText = `
            position: absolute;
            left: ${x}px;
            top: ${y}px;
            width: 4px;
            height: 4px;
            background: var(--primary-color);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9999;
            animation: sparkleAnimation 1s ease-out forwards;
        `;
        
        document.body.appendChild(sparkle);
        
        setTimeout(() => {
            if (sparkle.parentNode) {
                sparkle.remove();
            }
        }, 1000);
    }
    
    // Add sparkle animation
    const style = document.createElement('style');
    style.textContent = `
        @keyframes sparkleAnimation {
            0% {
                opacity: 1;
                transform: scale(0);
            }
            50% {
                opacity: 1;
                transform: scale(1);
            }
            100% {
                opacity: 0;
                transform: scale(0);
            }
        }
    `;
    document.head.appendChild(style);
});
</script>
{% endblock %}